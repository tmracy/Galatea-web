# 后端直连地址（生产或跨域时用）。开发时留空即可，走 vite 代理到 :8000。
# VITE_API_BASE=http://127.0.0.1:8000

# 设为 1 时强制 mock，不请求 Flask 后端。
VITE_USE_MOCK=

# vite dev 代理目标（默认 http://127.0.0.1:8000，与 gunicorn_conf.py 一致）
VITE_PROXY_TARGET=http://127.0.0.1:8001
